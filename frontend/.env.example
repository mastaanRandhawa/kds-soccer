# Development: leave empty to use Vite proxy /api → http://localhost:3001
# Production / GitHub Pages: full URL including /api suffix
VITE_API_URL=

# Static hosting base path. Use `/` for root hosting (custom domain root).
# For https://USERNAME.github.io/REPO/ set to `/REPO/`
VITE_BASE_PATH=/kds-soccer/
